import React from 'react';
import { Typography, Box } from '@mui/material';

const Reports = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Reports
      </Typography>
      {/* Reports interface will go here */}
    </Box>
  );
};

export default Reports; 