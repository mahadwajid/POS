import React from 'react';
import { Typography, Box } from '@mui/material';

const Billing = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Billing
      </Typography>
      {/* Billing interface will go here */}
    </Box>
  );
};

export default Billing; 